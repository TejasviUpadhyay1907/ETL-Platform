[build]
builder = "nixpacks"

[deploy]
startCommand = "python scripts/setup_database.py --skip-seed && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/v1/health/ping"
healthcheckTimeout = 60
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
