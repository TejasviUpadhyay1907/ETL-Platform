# =============================================================================
# Enterprise ETL & Data Quality Platform — Production Dependencies
# =============================================================================
# Pin exact versions for reproducible production builds.
# Update using: pip-compile requirements.in
# =============================================================================

# --- Web Framework -----------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.32.1          # ASGI server with uvloop + httptools
starlette==0.41.3                  # FastAPI dependency (pinned explicitly)

# --- Data Validation & Settings ----------------------------------------------
pydantic==2.10.3
pydantic-settings==2.7.0
pydantic[email]==2.10.3            # Email validation support
python-dotenv==1.0.1

# --- Database ----------------------------------------------------------------
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10            # PostgreSQL adapter
asyncpg==0.30.0                    # Async PostgreSQL driver (for future async support)

# --- Data Processing ---------------------------------------------------------
pandas==2.2.3
numpy==2.2.0
openpyxl==3.1.5                    # Excel read/write (.xlsx)
xlrd==2.0.1                        # Legacy Excel read (.xls)

# --- Scheduling --------------------------------------------------------------
apscheduler==3.10.4

# --- Logging -----------------------------------------------------------------
loguru==0.7.3
python-json-logger==2.0.7          # JSON formatter for stdlib logging

# --- Security ----------------------------------------------------------------
passlib[bcrypt]==1.7.4             # Password hashing
python-jose[cryptography]==3.3.0   # JWT tokens (future auth)
cryptography==44.0.0

# --- HTTP & Utilities --------------------------------------------------------
httpx==0.28.1                      # Async HTTP client (for testing and health checks)
python-multipart==0.0.20           # Multipart form data (file uploads)
aiofiles==24.1.0                   # Async file I/O
jinja2==3.1.4                      # HTML templating for dashboard
markupsafe==3.0.2                  # Jinja2 dependency

# --- Configuration -----------------------------------------------------------
pyyaml==6.0.2                      # YAML config file parsing

# --- Compression & Middleware -----------------------------------------------
brotli==1.1.0                      # Brotli compression for responses

# --- Monitoring & Observability ---------------------------------------------
prometheus-client==0.21.1          # Future: metrics exposure

# --- Dashboard (Phase 11) ---------------------------------------------------
streamlit==1.56.0                  # Operations dashboard UI
plotly==6.3.1                      # Interactive charts

# --- Email (future) ----------------------------------------------------------
# fastapi-mail==1.4.1
